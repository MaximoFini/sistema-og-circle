// Placeholder de la pantalla de login — destino real del redirect que hace
// `middleware.ts` (VGRP-17) cuando no hay sesión válida en rutas de
// `(app)`. Formulario real y flujo de auth: Bloque 2, fuera de alcance acá.
export default function LoginPage() {
  return <p>Login — próximamente</p>;
}
