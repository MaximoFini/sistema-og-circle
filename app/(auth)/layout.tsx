import type { ReactNode } from "react";

// Placeholder de layout para el grupo de rutas de autenticación
// (login, registro, reset de contraseña). Construcción real: Bloque 2.
export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
