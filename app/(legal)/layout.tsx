import type { ReactNode } from "react";

// Placeholder de layout para páginas legales. Contenido real pendiente
// de que se entregue el texto legal (fuera de alcance de este ticket).
export default function LegalLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
